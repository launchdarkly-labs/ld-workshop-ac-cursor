{
  "name": "factory-floor",
  "version": "1.1.0",
  "private": true,
  "description": "Side-scrolling visualization of an AutoFactory agent chain working a pull request. Node stdlib only, no dependencies, no build step.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs"
  }
}
